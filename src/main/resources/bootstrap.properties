# RESTFull app request URL to get info
# http://localhost:8081/info
server.port=8081

# Spring cloud server url & port
spring.cloud.config.uri=http://localhost:8090/

# This parameter is common for all application properties. If you need to change it,
# you can override this parameter in property files (see prod profile).
spring.cloud.config.name=my-lovely-filename